name: Publish to Naver Blog

on:
  workflow_dispatch: {}
  schedule:
    - cron: '0 * * * *'  # 매시 정각(UTC)

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run publisher
        env:
          NOTION_TOKEN: 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
          NOTION_DATABASE_ID: 2935abc12af8803c9950f9e5f84d9c76
          NAVER_CLIENT_ID: y6iD4sEK_JmhJgegNRTQ
          NAVER_CLIENT_SECRET: Fmgw6tM8wG 
          NAVER_ACCESS_TOKEN: le4LRnH2KQdgrBZH0w 
          NAVER_REFRESH_TOKEN: ${{ secrets.NAVER_REFRESH_TOKEN }}
          DEFAULT_CATEGORY_ID: ${{ secrets.DEFAULT_CATEGORY_ID }}
          TIMEZONE: Asia/Seoul
          DRY_RUN: 'false'
        run: |
          python -m src.main
